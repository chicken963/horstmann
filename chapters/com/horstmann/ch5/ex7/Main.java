package com.horstmann.ch5.ex7;

import java.util.Scanner;

/**
 * Для выполнения этого упражнения вам придется просмотреть исходный код класса java.util.Scanner. Если при
 * использовании класса Scanner операция ввода завершается неудачно, в этом классе перехватывается исключение в связи с
 * ошибками ввода данных и закрывается ресурс, из которого осуществляется ввод. Что произойдет, если ири закрытии
 * ресурса будет сгенерировано исключение? Каким образом эта реализация взаимодействует с обработкой подавляемых
 * исключений в операторе try с ресурсами?
 */
public class Main {
    Scanner scanner;
    /*в общем, в классе Scanner есть поле lastException. Если close() генерирует исключение, оно пишется туда, а затем
    подхватывается методом ioException(). Но вот как подавляемое взаимодействует с первичным - непонятно, ибо этот метод
     потом нигде не используется*/
}
